# Runtime dependencies. Everything runs offline with the default mock model;
# real providers (OpenAI/Anthropic/Ollama) need only the httpx client below.
langgraph>=1.2,<2
httpx>=0.27,<1
python-dotenv>=1,<2
